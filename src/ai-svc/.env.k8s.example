# K8s Integration Test Configuration
# Copy this file to .env.k8s and update with your Minikube/K8s cluster details

# Server Configuration - K8s NodePort access
# Get Minikube IP with: minikube ip
PORT=30082
HOST=<your-minikube-ip>

# Dapr Configuration - Not applicable for external tests
# Tests will hit the service directly via NodePort
DAPR_HOST=localhost
DAPR_HTTP_PORT=3500
DAPR_GRPC_PORT=50001
STATE_STORE_NAME=ai-statestore
PUBSUB_NAME=ai-pubsub

# Service URLs - K8s NodePort access
EVT_SVC_URL=http://<your-minikube-ip>:30082
CTX_SVC_URL=http://<your-minikube-ip>:30081

# NATS Configuration - Requires port-forward for external tests
# kubectl port-forward -n pubsub-mcp svc/nats 4222:4222
NATS_URL=nats://localhost:4222

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_BASE_URL=https://api.openai.com/v1  # Optional: Custom OpenAI endpoint