apiVersion: v1
kind: Service
metadata:
  name: dapr-scheduler
  labels:
    app: dapr-scheduler
    component: dapr
spec:
  type: ClusterIP
  ports:
  - port: 50006
    targetPort: 50006
    protocol: TCP
    name: scheduler
  selector:
    app: dapr-scheduler
