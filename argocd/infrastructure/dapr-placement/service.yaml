apiVersion: v1
kind: Service
metadata:
  name: dapr-placement
  labels:
    app: dapr-placement
    component: dapr
spec:
  type: ClusterIP
  ports:
  - port: 50006
    targetPort: 50006
    protocol: TCP
    name: placement
  selector:
    app: dapr-placement
