apiVersion: v1
kind: Service
metadata:
  name: ai-mcp-postgres
  labels:
    app: ai-mcp-postgres
    component: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: ai-mcp-postgres
